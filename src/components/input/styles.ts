import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    padding: 15,
    width: "100%",
    alignItems: "center",
    color: "#000",
    fontSize: 10,
    fontWeight: "bold",
    borderWidth: 1,
    borderColor: "orange",
  },
});
