import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  btn: {
    backgroundColor: "orange",
    padding: 15,
    width: "100%",
    alignItems: "center",
  },
  title: {
    color: "#000",
    fontSize: 15,
    fontWeight: "bold",
  },
});
